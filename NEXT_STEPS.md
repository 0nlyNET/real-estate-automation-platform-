# Next steps (in order)

Do NOT do all at once. One at a time.

1) Add booking link to tenant table
2) Replace hardcoded email content with templates
3) Add SMS (Twilio) alongside email
4) Rebuild sequences properly (new migration)
5) Add simple admin UI to view leads
6) Deploy to a VPS or Railway/Fly.io

