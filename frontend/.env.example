# Server-only API origin used by the same-origin /api/backend proxy.
# Do not rename this to NEXT_PUBLIC_*; browser code must not call the API origin directly.
BACKEND_API_URL=http://localhost:4000

# Canonical public origin used for metadata, robots.txt, and sitemap.xml.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
